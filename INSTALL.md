# Руководство по установке и запуску NoiseLab++

## 📋 Содержание

1. [Требования](#требования)
2. [Установка](#установка)
3. [Проверка установки](#проверка-установки)
4. [Запуск демонстраций](#запуск-демонстраций)
5. [Запуск тестов](#запуск-тестов)
6. [Запуск веб-интерфейса](#запуск-веб-интерфейса)
7. [Решение проблем](#решение-проблем)

---

## 🔧 Требования

### Системные требования

- **Python**: 3.8 или выше
- **ОС**: Linux, macOS или Windows
- **RAM**: Минимум 4 GB (рекомендуется 8 GB для 3-кубитных систем)
- **Процессор**: Любой современный процессор (многоядерный рекомендуется)

### Зависимости Python

Все необходимые библиотеки перечислены в `requirements.txt`:

```
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0
qiskit>=0.45.0
qiskit-aer>=0.13.0
cvxpy>=1.4.0
flask>=3.0.0
flask-cors>=4.0.0
plotly>=5.18.0
seaborn>=0.13.0
pandas>=2.1.0
tqdm>=4.66.0
pytest>=7.4.0
```

---

## 📥 Установка

### Шаг 1: Клонирование репозитория

```bash
cd /Users/timofei21/phisics
```

### Шаг 2: Создание виртуального окружения (рекомендуется)

```bash
python3 -m venv venv
source venv/bin/activate  # На macOS/Linux
# или
venv\Scripts\activate  # На Windows
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

Установка может занять несколько минут, особенно для CVXPY и Qiskit.

### Шаг 4: Добавление проекта в PYTHONPATH

```bash
export PYTHONPATH="${PYTHONPATH}:/Users/timofei21/phisics"
```

Для постоянного добавления в `.bashrc` или `.zshrc`:

```bash
echo 'export PYTHONPATH="${PYTHONPATH}:/Users/timofei21/phisics"' >> ~/.zshrc
source ~/.zshrc
```

---

## ✅ Проверка установки

Запустите простой тест для проверки установки:

```bash
python -c "import noiselab; print('NoiseLab++ установлен успешно!')"
```

Проверка импорта основных модулей:

```bash
python << EOF
from noiselab.core.states import QuantumState
from noiselab.channels.noise_models import DepolarizingChannel
from noiselab.tomography.qpt import QuantumProcessTomography
print("✓ Все модули импортированы успешно!")
EOF
```

---

## 🚀 Запуск демонстраций

### Основная демонстрация томографии

Запустите полную демонстрацию всех возможностей:

```bash
python examples/demo_tomography.py
```

**Что показывает демонстрация:**
1. Томография деполяризующего канала (1 кубит)
2. Томография amplitude damping канала
3. Статистический анализ (10 прогонов)
4. Двухкубитная томография

**Ожидаемый результат:**
- Process fidelity > 0.99 для 1-кубитных систем
- CPTP валидация: PASSED
- Точность оценки параметров < 0.01

**Время выполнения:**
- 1-кубитная томография: ~1-2 секунды
- 2-кубитная томография: ~10-20 секунд
- 10 прогонов: ~30 секунд

### Дополнительные примеры

#### Пример 1: Простая томография

```python
from noiselab.channels.noise_models import DepolarizingChannel
from noiselab.tomography.qpt import QuantumProcessTomography

# Создаём неизвестный канал
channel = DepolarizingChannel(p=0.1, n_qubits=1)

# Инициализируем QPT
qpt = QuantumProcessTomography(n_qubits=1, shots=1000)

# Проводим томографию
result = qpt.run_tomography(channel, reconstruction_method='LSQ')

# Выводим результат
print(f"Process Fidelity: {result.process_fidelity:.6f}")
```

#### Пример 2: Анализ различных моделей шума

```python
from noiselab.channels.noise_models import (
    DepolarizingChannel,
    AmplitudeDampingChannel,
    PhaseDampingChannel
)
from noiselab.tomography.qpt import QuantumProcessTomography

channels = [
    DepolarizingChannel(0.1, n_qubits=1),
    AmplitudeDampingChannel(0.2),
    PhaseDampingChannel(0.15)
]

qpt = QuantumProcessTomography(n_qubits=1, shots=1000)

for channel in channels:
    result = qpt.run_tomography(channel)
    print(f"{channel.name}: F = {result.process_fidelity:.6f}")
```

---

## 🧪 Запуск тестов

### Запуск всех тестов

```bash
pytest tests/ -v
```

### Запуск конкретных тестов

```bash
# Тесты ядра
pytest tests/test_core.py -v

# Тесты каналов
pytest tests/test_channels.py -v

# Тесты томографии
pytest tests/test_tomography.py -v
```

### Запуск тестов с покрытием

```bash
pytest tests/ --cov=noiselab --cov-report=html
```

Отчёт будет в `htmlcov/index.html`

---

## 🌐 Запуск веб-интерфейса

### Backend (Flask API)

```bash
cd web
python app.py
```

Сервер запустится на `http://localhost:5000`

### Frontend (React)

В отдельном терминале:

```bash
cd web/frontend
npm install
npm start
```

Приложение откроется в браузере на `http://localhost:3000`

### Использование веб-интерфейса

1. **Выбор числа кубитов**: 1, 2 или 3
2. **Создание канала**:
   - Выберите тип шума (Depolarizing, Amplitude Damping, etc.)
   - Задайте параметры или сгенерируйте случайный канал
3. **Настройка томографии**:
   - Число shots (100-10000)
   - Метод реконструкции (LSQ/MLE)
   - Ошибки считывания
4. **Запуск томографии**: Нажмите "Run QPT"
5. **Анализ результатов**:
   - Process fidelity
   - Choi matrix (heatmap)
   - Операторы Крауса
   - PTM visualization
   - Сравнение с моделями

---

## 🎯 Сценарии использования для презентации

### Сценарий 1: Оценка параметра известного канала

**Цель**: Показать точность реконструкции параметров шума

```python
python examples/demo_tomography.py
# Смотрим раздел "ДЕМОНСТРАЦИЯ 1"
```

**Презентация**:
- Задаём известный канал с p=0.1
- Проводим томографию
- Показываем: оценённый параметр ≈ 0.1 (точность ~0.001)
- Process fidelity > 0.99

### Сценарий 2: Идентификация типа шума

**Цель**: Определить тип неизвестного шума

```python
from noiselab.channels.random import random_cptp_channel
from noiselab.tomography.qpt import QuantumProcessTomography
from noiselab.metrics.validation import estimate_error_rates

# Генерируем случайный канал
unknown = random_cptp_channel(n_qubits=1, seed=42)

# Томография
qpt = QuantumProcessTomography(n_qubits=1, shots=2000)
result = qpt.run_tomography(unknown)

# Пробуем разные модели
for model in ['depolarizing', 'amplitude_damping', 'phase_damping']:
    est = estimate_error_rates(result.reconstructed_channel,
                               identity_channel,
                               error_model=model)
    print(f"{model}: fit_fidelity = {est['fit_fidelity']:.4f}")
```

### Сценарий 3: Влияние статистики

**Цель**: Показать зависимость точности от числа измерений

```python
shots_list = [100, 500, 1000, 5000, 10000]
channel = DepolarizingChannel(0.1, n_qubits=1)

for shots in shots_list:
    qpt = QuantumProcessTomography(n_qubits=1, shots=shots)
    result = qpt.run_tomography(channel)
    print(f"Shots: {shots:5d}, Fidelity: {result.process_fidelity:.6f}")
```

**Ожидаемый результат**:
- 100 shots: F ≈ 0.95
- 1000 shots: F ≈ 0.99
- 10000 shots: F ≈ 0.995

### Сценарий 4: Двухкубитные коррелированные шумы

**Цель**: Исследовать кросс-токи между кубитами

```python
from noiselab.channels.two_qubit_noise import CorrelatedNoise

channel = CorrelatedNoise(p_single=0.05, p_correlated=0.1,
                         correlation_type='XX')

qpt = QuantumProcessTomography(n_qubits=2, shots=1000)
result = qpt.run_tomography(channel)

print(f"Fidelity: {result.process_fidelity:.6f}")
print(f"Rank: {result.reconstructed_channel.kraus_rank()}")
```

---

## 🐛 Решение проблем

### Проблема 1: ImportError

**Ошибка**:
```
ImportError: No module named 'noiselab'
```

**Решение**:
```bash
export PYTHONPATH="${PYTHONPATH}:/Users/timofei21/phisics"
```

### Проблема 2: CVXPY не работает

**Ошибка**:
```
ModuleNotFoundError: No module named 'cvxpy'
```

**Решение**:
```bash
pip install cvxpy
# Если не работает, попробуйте:
conda install -c conda-forge cvxpy
```

### Проблема 3: Медленная томография

**Проблема**: 2-3 кубитная томография слишком долгая

**Решение**:
- Уменьшите число shots (100-500)
- Используйте LSQ вместо MLE
- Для 3 кубитов используйте мощный компьютер

### Проблема 4: Низкая fidelity

**Проблема**: Process fidelity < 0.9

**Возможные причины**:
1. Слишком мало shots → Увеличьте до 1000+
2. Численная нестабильность → Используйте LSQ
3. Ошибки в реконструкции → Проверьте CPTP условия

**Диагностика**:
```python
from noiselab.metrics.validation import analyze_tomography_quality

quality = analyze_tomography_quality(result)
print(quality)
```

---

## 📊 Ожидаемые показатели производительности

### Время выполнения (на стандартном ноутбуке)

| Операция | 1 кубит | 2 кубита | 3 кубита |
|----------|---------|----------|----------|
| QPT (1000 shots) | 1-2 сек | 5-10 сек | 30-60 сек |
| MLE реконструкция | 2-5 сек | 10-30 сек | 1-2 мин |
| 10 прогонов | 10-20 сек | 1-2 мин | 5-10 мин |

### Точность реконструкции

| Параметр | Целевое значение |
|----------|------------------|
| Process Fidelity (1 кубит, 1000 shots) | > 0.99 |
| Process Fidelity (2 кубита, 1000 shots) | > 0.95 |
| Точность оценки параметра | < 0.01 |
| CPTP error | < 1e-8 |

---

## 📞 Контакты и поддержка

При возникновении проблем:

1. Проверьте раздел "Решение проблем"
2. Убедитесь, что все зависимости установлены
3. Проверьте версию Python (должна быть >= 3.8)

---

## 🎓 Дополнительные ресурсы

### Теоретическая база

- **Quantum Process Tomography**: Nielsen & Chuang, "Quantum Computation and Quantum Information", Chapter 8
- **CPTP maps**: Kraus representation
- **Choi-Jamiolkowski isomorphism**: связь каналов и состояний

### Примеры из литературы

- **Depolarizing channel**: базовая модель шума в NISQ устройствах
- **Amplitude damping**: релаксация T₁
- **Phase damping**: декогеренция T₂

---

**Успешной работы с NoiseLab++! 🚀**
